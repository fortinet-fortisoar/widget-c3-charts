## What's New

- Resolved an issue where charts were rendering without displaying any data. This occurred particularly when a picklist or lookup field was selected as a data source. The widget now correctly handles these field types, ensuring data is displayed as expected in the C3 charts.